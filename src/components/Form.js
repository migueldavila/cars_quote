import React from 'react';

const Form = () => {
  return (
    <form>
      <div>
        <label>Marca</label>
        <select>
          <option value="">---Seleccione---</option>
        </select>
      </div>
    </form>
  );
};

export default Form;